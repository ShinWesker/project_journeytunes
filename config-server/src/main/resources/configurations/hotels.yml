eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 7000

spring:
  application:
    name: hotels
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/journeytunes_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
